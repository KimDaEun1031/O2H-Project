<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.company.mapper.KDmapper">
	<!-- 로그인 확인 -->
	<select id="selectByUser" resultType="com.company.domain.KDAuthVO">
		select userId,userName,auth, userInfo from user_board where userId=#{userId} and password=#{password}
	</select>
	
	<select id="selectByMy" resultType="com.company.domain.KDAuthVO">
		select userName,userId,password,address,phoneNumber,userEmail,userBirth,interest1,interest2,interest3 from user_board where userId=#{userId} and auth=#{auth}
	</select>
	
	<!-- 정보란 수정 -->
	<update id="updateUser">
		update user_board set userInfo=#{userInfo} where userId=#{userId} and auth=#{auth}
	</update>

	
	<!-- 회원가입 - register -->
	<insert id="insertRegister">
		insert into user_board(userName,userId,password,address,phoneNumber,userEmail,userBirth,interest1,interest2,interest3,auth,userInfo) 
		values(#{userName},#{userId},#{password},#{address},#{phoneNumber},#{userEmail},#{userBirth},#{favorite_activity[0]},#{favorite_activity[1]},#{favorite_activity[2]},#{auth},#{userInfo})
	</insert>
	
</mapper>